<h1>New Mixer</h1>

<%= render :partial => "shared/error_messages", :locals => {:target => @mixer} %>

<%= form_for @mixer do |f| %>
  <%= render :partial => "form", :locals =>{:f => f} %>
  
  <p class="buttons">
    <%= submit_tag "Save" %>
    <%= button_to_function "Reset", "$('#new_mixer')[0].reset()" %>
    <%= button_to_function "Cancel", "window.location.replace('#{mixers_path}')" %>
  </p>
<% end %>